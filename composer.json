{
    "name": "edgar-pc/test",
    "description": "Teste de programador",
    "autoload": {
        "psr-4": {
            "EdgarPc\\test\\": "src/"
        }
    },
    "authors": [
        {
            "name": "Edgar Filho"
        }
    ],
    "require-dev": {
        "phpunit/phpunit": "9.6"
    },
    "scripts": {
        "test": "phpunit",
        "lint": "for /r src %f in (*.php) do @(echo Verifying %f & php -l %f)"
    }
}
